import Footer from '@/components/Footer'; import Header from '@/components/Header'; import { fallbackReplies } from '@/lib/memes';
export const metadata = { title: '밈 모음', description: 'NEWMB.chat의 영어 키워드 밈 답변 모음' };
export default function Memes(){ return <main className="min-h-screen bg-black"><Header/><section className="mx-auto max-w-5xl px-4 py-10"><h1 className="text-4xl font-black">밈 모음</h1><p className="mt-3 text-white/55">SEO용 기본 콘텐츠입니다. 출시 후 계속 늘리면 검색 유입에 도움이 됩니다.</p><div className="mt-8 grid gap-4 md:grid-cols-2">{fallbackReplies.map((r)=><article key={r.keyword} className="glass rounded-3xl p-5"><h2 className="text-3xl font-black text-red-500">{r.keyword}</h2>{r.lines.map((l)=><p key={l} className="mt-2 text-lg font-bold">{l}</p>)}</article>)}</div></section><Footer/></main> }
